import React from 'react';
import classes from "./Footer.module.css";
const Footer = () => {
  return (
	<section className={`cc-footer py-1 ${classes["footer-pers"]}`}>
			<div className={`col-12 text-center py-1  text-muted ${classes["footer-pers"]}`}> 
					 
						Ministere de sante
					</div>
				
		</section>
  );
};

export default Footer;
